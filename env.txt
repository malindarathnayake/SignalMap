# SignalMap v3 — secrets / config
# Gitignored. Used by docker-compose.yml
# and by the local Node API process.
#
# Reference for all env vars: docs/SignalMap/spec.md §Config Schema
# Discovery findings: docs/SignalMap/_discovery/

# ═══════════════════════════════════════════════════════════════
# Brief feature (Phase 6)
# ═══════════════════════════════════════════════════════════════

# Perplexity Sonar Pro — global brief retrieval (clickbait-resistant + ≤20-domain allowlist).
# Verified in Phase 0 unit 0a.
PERPLEXITY_API_KEY=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
PERPLEXITY_MODEL=sonar-pro

# OpenRouter — single-pass brief synthesis (decided 2026-04-26 after real-workflow test).
# Architecture: Perplexity Sonar Pro retrieval (strict grounding prompt + citation
# revalidation) -> Sonnet 4.6 synthesizes the final brief. Sonnet was the only
# tested model that ignored Perplexity's hallucinated context and grounded its
# output in the real local_signals when citations were missing or untrusted.
#
# Cost per brief (real-workflow test): ~$0.021 ($0.011 Sonnet + $0.011 Perplexity)
# at $5/day budget = ~240 briefs/day capacity. Acceptable for v1 single-tenant.
#
# 2-pass synth->moderate architecture was REJECTED because:
#   - Reasoning-tier models (Nemotron-super, Kimi K2.6, Gemini 3.1 Pro) all leak
#     chain-of-thought into the response, eating tokens without producing JSON
#   - Moderator dominates total cost regardless of draft quality
#   - Cheap-draft + premium-polish saves nothing in practice
OPENROUTER_API_KEY=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
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
SIGNALMAP_BRIEF_MODEL=anthropic/claude-sonnet-4.6

# Brief budget + cron interval (Phase 6c)
# Architecture (decided 2026-04-26): server-side background cron is the SOLE
# writer of the global brief. Frontend reads cached value, never triggers
# generation. Auth is handled by Cloudflare ZTNA at the edge — no app-level auth.
#
# Realistic daily cost ceiling: ~$1-1.50 (48 briefs * $0.017 + per-event clicks
# + collector classify). $2 budget gives ~30% headroom.
SIGNALMAP_DAILY_LLM_BUDGET_USD=2.00
SIGNALMAP_BRIEF_REFRESH_MINUTES=30

# Per-event brief stampede protection (only this endpoint can be hit
# simultaneously by multiple users on a fresh event). Global brief has no
# stampede path because cron is the sole writer.
SIGNALMAP_BRIEF_PER_EVENT_LOCK_TIMEOUT_SECONDS=30
SIGNALMAP_BRIEF_PER_EVENT_STAMPEDE_POLL_MS=200
SIGNALMAP_BRIEF_PER_EVENT_RATE_LIMIT_PER_MIN=20

# Manual "Refresh now" button in BriefStrip is gated behind this token.
# Set to a long random string and share only with operators. Coworkers see
# the button but it's hidden unless their browser sends X-SignalMap-Admin-Token.
SIGNALMAP_ADMIN_TOKEN=

# ═══════════════════════════════════════════════════════════════
# Collector (Phase 2 onwards)
# ═══════════════════════════════════════════════════════════════

CLOUDFLARE_API_TOKEN=cfut_PIVze28AASVGLxGg12sDADdspKhXXuTbypqZMuRwf91d39a5
SIGNALMAP_RSS_POLL_MINUTES=15
SIGNALMAP_VECTOR_ENABLED=true

# ═══════════════════════════════════════════════════════════════
# Redis (Phase 2 — direct ioredis after redis-rest is dropped)
# ═══════════════════════════════════════════════════════════════

# Defaults to compose service (redis://signalmap-redis:6379) — uncomment to override.
# REDIS_URL=redis://signalmap-redis:6379
# REDIS_PASSWORD=

# Legacy compose only — used by current redis-rest shim, will be removed in Phase 2c.
REDIS_TOKEN=local-dev-token

# ═══════════════════════════════════════════════════════════════
# SSE (Phase 3)
# ═══════════════════════════════════════════════════════════════

SSE_HEARTBEAT_SECONDS=20
SSE_REPLAY_RING_SIZE=1000
SSE_REPLAY_RING_TTL_SECONDS=600
SSE_RECONNECT_RETRY_MIN_MS=5000
SSE_RECONNECT_RETRY_MAX_MS=15000

# ═══════════════════════════════════════════════════════════════
# Container
# ═══════════════════════════════════════════════════════════════

SIGNALMAP_PORT=3000
LOCAL_API_PORT=46123
LOG_LEVEL=info
