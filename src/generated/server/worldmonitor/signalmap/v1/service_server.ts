// Code generated by protoc-gen-ts-server. DO NOT EDIT.
// source: worldmonitor/signalmap/v1/service.proto

export interface ListSignalMapEventsRequest {
  startMs: number;
  endMs: number;
  categories: string[];
  watchRegions: string[];
  watchProviders: string[];
  watchlistOnly: boolean;
}

export interface ListSignalMapEventsResponse {
  events: SignalMapEvent[];
  sourceHealth: SignalMapSourceHealth[];
  fetchedAt: number;
  upstreamUnavailable: boolean;
}

export interface SignalMapEvent {
  id: string;
  category: string;
  severity: string;
  title: string;
  summary: string;
  tags: string[];
  startedAt?: string;
  endedAt?: string;
  lastObservedAt: string;
  locations: SignalMapLocation[];
  sources: SignalMapSource[];
  confidence: number;
  provider?: string;
  kind: string;
  watchlistMatch: boolean;
  markerEligible: boolean;
}

export interface SignalMapLocation {
  name: string;
  countryIso2?: string;
  lat?: number;
  lon?: number;
  scope: string;
  confidence: number;
  evidence?: string;
}

export interface SignalMapSource {
  id: string;
  label: string;
  url?: string;
  tier?: number;
  verified?: boolean;
  fetchedAt?: string;
}

export interface SignalMapSourceHealth {
  id: string;
  label: string;
  status: string;
  fetchedAt: number;
  eventCount: number;
  detail: string;
}

export interface FieldViolation {
  field: string;
  description: string;
}

export class ValidationError extends Error {
  violations: FieldViolation[];

  constructor(violations: FieldViolation[]) {
    super("Validation failed");
    this.name = "ValidationError";
    this.violations = violations;
  }
}

export class ApiError extends Error {
  statusCode: number;
  body: string;

  constructor(statusCode: number, message: string, body: string) {
    super(message);
    this.name = "ApiError";
    this.statusCode = statusCode;
    this.body = body;
  }
}

export interface ServerContext {
  request: Request;
  pathParams: Record<string, string>;
  headers: Record<string, string>;
}

export interface ServerOptions {
  onError?: (error: unknown, req: Request) => Response | Promise<Response>;
  validateRequest?: (methodName: string, body: unknown) => FieldViolation[] | undefined;
}

export interface RouteDescriptor {
  method: string;
  path: string;
  handler: (req: Request) => Promise<Response>;
}

export interface SignalMapServiceHandler {
  listSignalMapEvents(ctx: ServerContext, req: ListSignalMapEventsRequest): Promise<ListSignalMapEventsResponse>;
}

export function createSignalMapServiceRoutes(
  handler: SignalMapServiceHandler,
  options?: ServerOptions,
): RouteDescriptor[] {
  return [
    {
      method: "GET",
      path: "/api/signalmap/v1/list-signal-map-events",
      handler: async (req: Request): Promise<Response> => {
        try {
          const pathParams: Record<string, string> = {};
          const url = new URL(req.url, "http://localhost");
          const params = url.searchParams;
          const body: ListSignalMapEventsRequest = {
            startMs: Number(params.get("start_ms") ?? "0"),
            endMs: Number(params.get("end_ms") ?? "0"),
            categories: params.getAll("categories"),
            watchRegions: params.getAll("watch_regions"),
            watchProviders: params.getAll("watch_providers"),
            watchlistOnly: params.get("watchlist_only") === "true",
          };
          if (options?.validateRequest) {
            const bodyViolations = options.validateRequest("listSignalMapEvents", body);
            if (bodyViolations) {
              throw new ValidationError(bodyViolations);
            }
          }

          const ctx: ServerContext = {
            request: req,
            pathParams,
            headers: Object.fromEntries(req.headers.entries()),
          };

          const result = await handler.listSignalMapEvents(ctx, body);
          return new Response(JSON.stringify(result as ListSignalMapEventsResponse), {
            status: 200,
            headers: { "Content-Type": "application/json" },
          });
        } catch (err: unknown) {
          if (err instanceof ValidationError) {
            return new Response(JSON.stringify({ violations: err.violations }), {
              status: 400,
              headers: { "Content-Type": "application/json" },
            });
          }
          if (options?.onError) {
            return options.onError(err, req);
          }
          const message = err instanceof Error ? err.message : String(err);
          return new Response(JSON.stringify({ message }), {
            status: 500,
            headers: { "Content-Type": "application/json" },
          });
        }
      },
    },
  ];
}

